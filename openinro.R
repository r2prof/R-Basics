# To install openintro package, un comment the following command and press enter.
# Note: You only need to install this package once for this course.

install.packages("openintro")

# Load the package
library(openintro)
data()

# Load the `loan50` dataset
data(loan50)

# View the dataset
View(loan50)

# Load first six rows for `loan50` dataset
head(loan50)

# Look at the structure of the dataset
str(loan50)



