# How to load the built-in data sets in R?-----

# Load the list of built-in data sets
data()

# Load mtcars data set
data(mtcars)

# Load the first six rows of mtcars data
head(mtcars)

# Load the complete data set in console
mtcars

# Load iris data set
data(iris)

# View the first six rows of the data
head(iris)

